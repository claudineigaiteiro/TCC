unit Controlers.Aneometro;

interface

uses

  Horse, Horse.Jhonson, Horse.CORS;

procedure postAneometro(Req: THorseRequest, Proc: TProc);

implementation

procedure postAneometro(Req: THorseRequest, Proc: TProc);
var
  // LJson: TJSONObject;
  LText: String;
begin
  // LJson := TJSONObject.Create;
  Try
    LText := Req.Body;
    // LJson := StrTo
  Finally
    // LJson.Free;
  End;

end

end.
